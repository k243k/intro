# Hero ショーケース画像の差し込み手順

1. LPスクショ（スマホ縦・推奨 比率 150:320 前後 / 例: 600×1280px）をこのフォルダに置く。
   例: 01.png, 02.png, ...
2. `src/data/showcase.ts` の各項目に `src: '/showcase/01.png'` を追加する。
   src が無い項目は CSS製の「デザイン例」プレースホルダが表示される。
3. 上段/下段は配列を半分に分割して自動振り分け。枚数は偶数推奨（左右バランス）。
